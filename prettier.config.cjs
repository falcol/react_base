// .prettierrc.js
module.exports = {
    semi: false, // Not use semicolon at the end of line
    trailingComma: 'all', // Add trailing commas where valid in ES5 (objects, arrays, etc.)
    singleQuote: true, // Use single quotes instead of double quotes
    printWidth: 100, // Maximum line length before wrapping
    tabWidth: 2, // Number of spaces per indentation level
    useTabs: false, // Use spaces instead of tabs
    bracketSpacing: true, // Add spaces inside object braces
    arrowParens: 'always', // Always include parentheses for arrow function (x) => ...
    endOfLine: 'auto', // Automatically adjust line endings based on the operating system
  }
